INSERT INTO sb-sandbox-usuarios.sandbox_cumplimiento.Score_de_terceros_Tabla_D
SELECT *
FROM sb-sandbox-usuarios.sandbox_cumplimiento.Score_de_terceros_Tabla_C src
WHERE NOT EXISTS (
    SELECT 1
    FROM sb-sandbox-usuarios.sandbox_cumplimiento.Score_de_terceros_Tabla_D tgt
    WHERE tgt.TIPO_DOCUMENTO = src.TIPO_DOCUMENTO
      AND tgt.KEY_ID = src.KEY_ID
      AND tgt.CODIGO_COMPANIA = src.CODIGO_COMPANIA
      AND tgt.FECHA_EJECUCION = src.FECHA_EJECUCION
);
